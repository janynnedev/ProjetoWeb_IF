@model IEnumerable<Projeto1_IF.Models.TbCategoriaMedicamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InformacaoComplementar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TbMedicamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(model => item.InformacaoComplementar)
            </td>
            <td>
                @Html.DisplayFor(model => item.TbMedicamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategoriaMedicamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategoriaMedicamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategoriaMedicamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
